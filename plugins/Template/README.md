# Plugin template

Copy `example.js` into a category, change its command metadata and handler, then add the relative path to the root `plugins.config.json` allow-list.

Keep commands small, validate user input, and handle external API failures with a friendly response.
